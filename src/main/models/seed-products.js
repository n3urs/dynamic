/**
 * Seed default gym products — blank slate, gym owner configures in Settings → Products
 */

function seedProducts() {
  // No defaults — gyms set up their own products during onboarding
  return 0;
}

module.exports = { seedProducts };
